<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="attachmentMapper">

    <insert id="insertAttachment">
    	INSERT INTO attachment (attachmentid, fileorder, filename, fileuuid, fileext, ismain, recipeid, recipestepid, announcementid, questionid)
    		VALUES(#{attachmentid}, #{fileorder}, #{filename}, #{fileuuid}, #{fileext}, #{ismain}, #{recipeid}, #{recipestepid}, #{announcementid}, #{questionid})
    </insert>
    
    <delete id="deleteAttachment">
    	DELETE FROM attachment
    	WHERE attachmentid = #{attachmentid} 
    </delete>
</mapper>