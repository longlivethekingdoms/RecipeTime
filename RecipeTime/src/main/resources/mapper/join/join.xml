<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="join">

    <!-- 회원가입 -->
    <insert id="add" parameterType="com.recipetime.find.Model.User">
        INSERT INTO recipeuser
        (userid, userpw, nickname, username, useremail, usertel, userbirth, signupdate, accesslevel)
        VALUES (#{userid}, #{userpw}, #{nickname}, #{username}, #{useremail}, #{usertel}, #{userbirth}, NOW(), 'normal')
    </insert>

</mapper>